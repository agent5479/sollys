# Placeholder Cloud Functions

Callable names: `listConsignments`, `getTracking`, `scanConsignment`, `listFleet`, `reserveCapacity`.

This folder is a contract stub for a later Firebase project (`sollys-prod` is a name only). The pitch app uses the showcase connector in `web/src/shared/connectors.ts`.

Do not commit service-account JSON. Fill `generated/env/functions.params.example.env` locally after a human provisions Firebase.
