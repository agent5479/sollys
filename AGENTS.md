# Agent notes

This repo is a **white-label engagement kit**. The operating prompt is [`whitelabel.md`](whitelabel.md). The always-on Cursor rule [`.cursor/rules/whitelabel-engagement.mdc`](.cursor/rules/whitelabel-engagement.mdc) requires Plan-mode interview (stack → surfaces → add-ons → terminology → brand gaps → infra boundary) before coding. Prefer the `@whitelabel/pivot-kit` CLI (`wl`) for intake drafts, generate, brand/SEO gates, and human infra checklists. Live Firebase, Apps Script, Calendar, and secret values stay human.

**Preserve §L3:** generated `REBRAND_PROMPT.md` is a post-interview apply aid only. Themed content never ships in toolkit defaults; brand placeholders live under `templates/brand/` so the multiproject folder is self-contained.
