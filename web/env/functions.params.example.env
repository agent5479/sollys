# Cloud Functions params / secrets (names from §G1)
# Set via firebase functions:config / params / secrets — do not commit values
FORM_ENDPOINT=
FUNCTIONS_WEBHOOK_SECRET=
SIGN_IN_URL=https://www.sollys.co.nz/app/signin/
CALLABLE_ORIGINS=https://www.example.com,http://localhost:5173,http://127.0.0.1:5173
TIME_ZONE=Pacific/Auckland
FUNCTIONS_REGION=australia-southeast1
